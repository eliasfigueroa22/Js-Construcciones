# Airtable credentials (only needed for fresh extraction)
AIRTABLE_API_TOKEN=your_token_here
AIRTABLE_BASE_ID=your_base_id_here

# DuckDB path (defaults to sample DB if not set)
DUCKDB_PATH=v2_analytics/warehouse/js_construcciones.duckdb
